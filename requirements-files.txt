# Local-files corpus extras (PDF / DOCX / PPTX ingestion).
# Install with:  pip install -r requirements.txt -r requirements-files.txt
pdfplumber>=0.11      # PDF text extraction (layout-aware)
python-docx>=1.1      # .docx
python-pptx>=0.6      # .pptx
openpyxl>=3.1         # .xlsx
# tiktoken (already a transitive dep) is used for token-accurate chunking.
